"use client";import { motion } from "framer-motion";import { fadeUp, staggerContainer } from "@/animations/motion";import { SectionHeading } from "@/components/ui/SectionHeading";const points = [  "Built a strong foundation in Python, Statistics, Data Analysis, Machine Learning, and Data Visualization through real-world projects.",  "Currently architecting end-to-end ML pipelines and production-grade data systems.",  "Focused on building intelligent solutions that turn messy data into measurable impact."];export function About() {  return (    <section id="about" className="section-shell py-8 sm:py-[60px]">      <SectionHeading        eyebrow="About"        title="A focused journey into applied intelligence."        description="Sachin Masti is a Data Scientist passionate about solving problems using data and technology."      />      <motion.div        variants={staggerContainer}        initial="hidden"        whileInView="visible"        viewport={{ once: true, margin: "-80px" }}        className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3"      >        {points.map((point) => (            <motion.div              key={point}              variants={fadeUp}              className="premium-border p-6"            >            <p className="text-subheading font-regular leading-[1.5] tracking-[0.025em] text-ash">              {point}            </p>          </motion.div>        ))}      </motion.div>    </section>  );}