"use client";import { motion } from "framer-motion";import { FiArrowUpRight } from "react-icons/fi";import { fadeUp, staggerContainer } from "@/animations/motion";import { SectionHeading } from "@/components/ui/SectionHeading";import { featuredPosts } from "@/data/blog";export function Blog() {  return (    <section id="blog" className="section-shell py-6 sm:py-10">      <SectionHeading        eyebrow="Blog"        title="Writing that turns learning into signal."        description="Featured posts from Medium, with data shape ready for future RSS integration."      />      <motion.div        variants={staggerContainer}        initial="hidden"        whileInView="visible"        viewport={{ once: true, margin: "-80px" }}        className="grid gap-3 md:grid-cols-2"      >        {featuredPosts.map((post, i) => (          <motion.article            key={post.title}            variants={fadeUp}            className="group relative flex flex-col overflow-hidden rounded-[20px] border border-white/[0.06] bg-white/[0.03] p-3 sm:p-4 transition-all hover:border-white/[0.12] hover:bg-white/[0.06]"          >            <div className="absolute inset-x-0 top-0 h-[2px] bg-gradient-to-r from-amber-spark to-plum-voltage opacity-60" />            <p className="mb-3 text-[10px] font-semibold uppercase tracking-[0.08em] text-amber-spark">              {post.platform}            </p>            <h3 className="text-base font-medium leading-[1.3] tracking-[-0.01em] text-bone">              {post.title}            </h3>            <p className="mt-1.5 text-xs font-regular leading-[1.5] tracking-[0.02em] text-smoke line-clamp-2">              {post.description}            </p>            <div className="mt-auto pt-3">              <a                href={post.href}                target="_blank"                rel="noreferrer"                className="inline-flex items-center gap-1.5 text-[11px] font-semibold uppercase tracking-[0.06em] text-smoke transition-colors hover:text-plum-voltage"                data-cursor="magnetic"              >                Read On Medium <FiArrowUpRight size={14} className="transition-transform group-hover:translate-x-0.5" />              </a>            </div>          </motion.article>        ))}      </motion.div>    </section>  );}