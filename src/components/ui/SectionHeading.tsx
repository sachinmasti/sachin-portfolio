"use client";import { motion } from "framer-motion";import { staggerContainer } from "@/animations/motion";type SectionHeadingProps = {  eyebrow: string;  title: string;  description?: string;};export function SectionHeading({ eyebrow, title, description }: SectionHeadingProps) {  return (    <motion.div      variants={staggerContainer}      initial="hidden"      whileInView="visible"      viewport={{ once: true, margin: "-100px" }}      className="mx-auto mb-8 max-w-3xl text-center sm:mb-[60px]"    >      <motion.p        variants={{          hidden: { opacity: 0, y: 20 },          visible: { opacity: 1, y: 0, transition: { duration: 0.6, ease: [0.22, 1, 0.36, 1] } }        }}        className="mb-3 text-caption font-semibold uppercase tracking-[0.05em] text-plum-voltage sm:mb-4"      >        {eyebrow}      </motion.p>      <motion.h2        variants={{          hidden: { clipPath: "inset(0 100% 0 0)" },          visible: {            clipPath: "inset(0 0% 0 0)",            transition: { duration: 1.2, delay: 0.15, ease: [0.22, 1, 0.36, 1] }          }        }}        className="text-balance text-[1.25rem] font-extralight leading-[1.1] tracking-[-0.04em] text-bone sm:text-heading lg:text-heading-lg"      >        {title}      </motion.h2>      {description ? (        <motion.p          variants={{            hidden: { opacity: 0, y: 20 },            visible: { opacity: 1, y: 0, transition: { duration: 0.7, delay: 0.4, ease: [0.22, 1, 0.36, 1] } }          }}          className="mx-auto mt-3 max-w-2xl text-sm font-regular leading-[1.5] tracking-[0.025em] text-smoke sm:mt-5 sm:text-subheading"        >          {description}        </motion.p>      ) : null}    </motion.div>  );}