"use client";import { motion, useScroll, useSpring } from "framer-motion";import { useEffect, useRef, useState } from "react";const sections = [  { id: "home", label: "Home" },  { id: "about", label: "About" },  { id: "story", label: "Story" },  { id: "skills", label: "Skills" },  { id: "projects", label: "Projects" },  { id: "github", label: "GitHub" },  { id: "blog", label: "Blog" },  { id: "contact", label: "Contact" }];function RocketIcon({ className }: { className?: string }) {  return (    <svg      viewBox="0 0 24 24"      fill="none"      stroke="currentColor"      strokeWidth="1.5"      strokeLinecap="round"      strokeLinejoin="round"      className={className}    >      <path d="M4.5 16.5c-1.5 1.26-2 5-2 5s3.74-.5 5-2c.71-.84.7-2.13-.09-2.91a2.18 2.18 0 0 0-2.91-.09z" />      <path d="M12 15l-3-3a22 22 0 0 1 2-3.95A12.88 12.88 0 0 1 22 2c0 2.72-.78 7.5-6 11a22.35 22.35 0 0 1-4 2z" />      <path d="M9 12H4s.55-3.03 2-4c1.62-1.08 5 0 5 0" />      <path d="M12 15v5s3.03-.55 4-2c1.08-1.62 0-5 0-5" />    </svg>  );}function StarIcon({ className }: { className?: string }) {  return (    <svg      viewBox="0 0 24 24"      fill="currentColor"      className={className}    >      <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z" />    </svg>  );}export function ScrollProgress() {  const { scrollYProgress } = useScroll();  const smoothProgress = useSpring(scrollYProgress, { stiffness: 100, damping: 30 });  const [activeIdx, setActiveIdx] = useState(0);  const rocketY = useRef(0);  const containerRef = useRef<HTMLDivElement>(null);  const totalSections = sections.length;  useEffect(() => {    const unsub = smoothProgress.on("change", (v) => {      const idx = Math.min(totalSections - 1, Math.round(v * (totalSections - 1)));      setActiveIdx(idx);      rocketY.current = v;    });    return () => unsub();  }, [smoothProgress, totalSections]);  return (    <div className="fixed right-6 top-1/2 z-50 hidden -translate-y-1/2 lg:block">      <div        ref={containerRef}        className="relative flex flex-col items-center py-2"      >        {/* Glow line */}        <div className="absolute inset-y-0 left-1/2 w-px -translate-x-1/2 bg-gradient-to-b from-plum-voltage via-lichen to-amber-spark opacity-20" />        {/* Rocket position indicator */}        <motion.div          className="absolute left-1/2 z-10 -translate-x-1/2"          animate={{            y: activeIdx * 56,            scale: [1, 1.1, 1],          }}          transition={{            y: { type: "spring", stiffness: 150, damping: 20 },            scale: { duration: 2, repeat: Infinity, ease: "easeInOut" }          }}          style={{ top: -8 }}        >          <div className="relative">            <RocketIcon className="h-5 w-5 text-bone drop-shadow-[0_0_8px_rgba(33,241,168,0.8)]" />            {/* Exhaust glow */}            <div className="absolute -bottom-2 left-1/2 h-3 w-1 -translate-x-1/2 rounded-full bg-gradient-to-b from-amber-spark to-transparent blur-sm" />          </div>        </motion.div>        {/* Section markers */}        {sections.map((section, i) => {          const isActive = activeIdx === i;          const dist = Math.abs(activeIdx - i);          return (            <a              key={section.id}              href={`#${section.id}`}              className="group relative flex items-center py-5 first:pt-0 last:pb-0"            >              <div                className="flex items-center gap-3 transition-all duration-500"                style={{ opacity: dist <= 1 ? 1 : 0.2 }}              >                {isActive ? (                  <div className="relative flex h-5 w-5 items-center justify-center">                    <motion.div                      className="absolute inset-0 rounded-full bg-plum-voltage"                      animate={{ scale: [1, 1.5, 1], opacity: [0.4, 0.1, 0.4] }}                      transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}                    />                    <StarIcon className="relative h-3.5 w-3.5 text-plum-voltage drop-shadow-[0_0_6px_rgba(33,241,168,0.8)]" />                  </div>                ) : (                  <span                    className="block rounded-full bg-smoke/40 transition-all duration-500"                    style={{                      width: 6,                      height: 6,                    }}                  />                )}                <motion.span                  className="text-caption font-regular tracking-[0.021em] text-smoke/60"                  animate={{                    opacity: isActive ? 1 : 0,                    x: isActive ? 0 : -4,                  }}                  transition={{ duration: 0.3 }}                >                  {section.label}                </motion.span>              </div>            </a>          );        })}      </div>    </div>  );}