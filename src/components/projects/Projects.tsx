"use client";import { motion } from "framer-motion";import { FiArrowUpRight, FiGithub, FiExternalLink } from "react-icons/fi";import { fadeUp } from "@/animations/motion";import { SectionHeading } from "@/components/ui/SectionHeading";import { projects } from "@/data/projects";const ACCENTS = ["from-plum-voltage to-lichen", "from-amber-spark to-plum-voltage", "from-lichen to-amber-spark", "from-plum-voltage to-amber-spark", "from-lichen to-plum-voltage"];export function Projects() {  return (    <section id="projects" className="section-shell py-6 sm:py-10">      <SectionHeading        eyebrow="Projects"        title="Selected Work"        description="Real-world Data Science and Machine Learning projects."      />      <div className="grid gap-3 lg:grid-cols-2">        {projects.map((project, i) => (          <motion.article            key={project.title}            variants={fadeUp}            initial="hidden"            whileInView="visible"            viewport={{ once: true, margin: "-80px" }}            className={`group relative flex flex-col overflow-hidden rounded-[20px] border border-white/[0.06] bg-white/[0.03] p-3 sm:p-4 transition-all hover:border-white/[0.12] hover:bg-white/[0.06] ${projects.length % 2 !== 0 && i === projects.length - 1 ? "lg:col-span-2 lg:max-w-[50%] lg:mx-auto" : ""}`}          >            <div className={`absolute inset-x-0 top-0 h-[2px] bg-gradient-to-r ${ACCENTS[i % ACCENTS.length]} opacity-60`} />            <div className="mb-3 flex items-start justify-between gap-4">              <span className="rounded-full bg-white/[0.06] px-2.5 py-0.5 text-[10px] font-semibold uppercase tracking-[0.08em] text-ash">                {project.status}              </span>              <FiArrowUpRight className="text-base text-ash transition-transform group-hover:translate-x-0.5 group-hover:-translate-y-0.5" />            </div>            <h3 className="text-base font-medium leading-[1.3] tracking-[-0.01em] text-bone">              {project.title}            </h3>            <p className="mt-1.5 text-xs font-regular leading-[1.5] tracking-[0.02em] text-smoke line-clamp-2">              {project.description}            </p>            <div className="mt-auto pt-3 flex flex-wrap gap-1.5">              {project.tags.map((tag) => (                <span                  key={tag}                  className="rounded-full bg-white/[0.04] px-2 py-0.5 text-[10px] font-medium uppercase tracking-[0.06em] text-ash"                >                  {tag}                </span>              ))}            </div>            {(project.github || project.live) && (              <div className="mt-3 flex flex-wrap gap-3 border-t border-white/[0.05] pt-3">                {project.github && (                  <a                    href={project.github}                    target="_blank"                    rel="noopener noreferrer"                    className="flex items-center gap-1.5 text-[11px] font-semibold uppercase tracking-[0.06em] text-smoke transition-colors hover:text-plum-voltage"                  >                    <FiGithub size={14} />                    Code                  </a>                )}                {project.live && (                  <a                    href={project.live}                    target="_blank"                    rel="noopener noreferrer"                    className="flex items-center gap-1.5 text-[11px] font-semibold uppercase tracking-[0.06em] text-smoke transition-colors hover:text-lichen"                  >                    <FiExternalLink size={16} />                    Live Demo                  </a>                )}              </div>            )}          </motion.article>        ))}      </div>    </section>  );}